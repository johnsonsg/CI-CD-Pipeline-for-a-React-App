import React from "react";
import ReactDOM from "react-dom";

const App = () => {
  return (
    <div>
      <h1>CI/CD Pipeline for a React App!</h1>
      <h2>AWS CodePipeline BoilerPlate </h2>
    </div>
  );
};

ReactDOM.render(<App />, document.getElementById("root"));